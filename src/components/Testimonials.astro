---
import Icon from './Icon.astro';

const testimonials = [
  {
    id: 1,
    name: 'María González',
    position: 'CEO',
    company: 'González & Asociados',
    image:
      'https://images.unsplash.com/photo-1494790108755-2616b612b786?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'Tecnodespegue transformó completamente nuestra presencia digital. El sitio web que desarrollaron no solo es hermoso, sino que también aumentó nuestras conversiones en un 150%. Su profesionalismo y atención al detalle son excepcionales.',
    project: 'Sitio web corporativo y sistema de gestión de clientes',
  },
  {
    id: 2,
    name: 'Carlos Rodríguez',
    position: 'Fundador',
    company: 'EcoTech Solutions',
    image:
      'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'Necesitábamos una aplicación web compleja para gestionar nuestros procesos internos. El equipo de Tecnodespegue no solo entendió perfectamente nuestras necesidades, sino que propuso mejoras que ni siquiera habíamos considerado.',
    project: 'Aplicación web de gestión empresarial',
  },
  {
    id: 3,
    name: 'Ana Martínez',
    position: 'Directora de Marketing',
    company: 'Boutique Luna',
    image:
      'https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'El e-commerce que desarrollaron para nuestra boutique es simplemente perfecto. Las ventas online se triplicaron en los primeros tres meses. La integración con redes sociales y el sistema de pagos funciona impecablemente.',
    project: 'E-commerce y marketing digital',
  },
  {
    id: 4,
    name: 'Roberto Silva',
    position: 'Gerente General',
    company: 'Transportes La Pampa',
    image:
      'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'La app móvil que desarrollaron para nuestros conductores revolucionó nuestra operación. Ahora podemos rastrear envíos en tiempo real y la comunicación con clientes mejoró drasticamente. ROI excepcional.',
    project: 'Aplicación móvil de logística',
  },
  {
    id: 5,
    name: 'Laura Pérez',
    position: 'Propietaria',
    company: 'Café Central',
    image:
      'https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'Como pequeña empresa, pensé que no podría acceder a tecnología de calidad. Tecnodespegue me demostró lo contrario. Su sistema de pedidos online nos ayudó a sobrevivir durante la pandemia y ahora somos más fuertes que nunca.',
    project: 'Sistema de pedidos online',
  },
  {
    id: 6,
    name: 'Diego Fernández',
    position: 'CTO',
    company: 'FinTech Pampa',
    image:
      'https://images.unsplash.com/photo-1560250097-0b93528c311a?w=80&h=80&fit=crop&crop=face&auto=format&q=80',
    rating: 5,
    content:
      'Trabajar con Tecnodespegue fue una decisión acertada. Su expertise en backend y APIs nos permitió lanzar nuestro producto fintech de manera segura y escalable. Su conocimiento en seguridad es impresionante.',
    project: 'Backend seguro para aplicación financiera',
  },
];

const stats = [
  {
    number: '98%',
    label: 'Satisfacción del Cliente',
    icon: 'Heart',
  },
  {
    number: '150%',
    label: 'Promedio de Aumento en Conversiones',
    icon: 'TrendingUp',
  },
  {
    number: '50+',
    label: 'Proyectos Exitosos',
    icon: 'Award',
  },
  {
    number: '24/7',
    label: 'Soporte Técnico',
    icon: 'Clock',
  },
];
---

<section id="testimonios" class="py-24 lg:py-32 bg-gray-50 overflow-hidden">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold text-gray-900 leading-[1.15] md:leading-[1.1] tracking-tight mb-8">
        Lo que dicen nuestros
        <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
          clientes
        </span>
      </h2>
      <p class="text-xl md:text-2xl text-gray-600 leading-[1.7] mb-12">
        La satisfacción de nuestros clientes es nuestra mejor carta de presentación. 
        Descubre cómo hemos transformado negocios en toda La Pampa y Argentina.
      </p>
    </div>

    <!-- Stats Grid -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-20">
      {stats.map((stat, index) => (
        <div class="text-center group">
          <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-200 hover:shadow-lg transition-all duration-300 hover:-translate-y-1">
            <div class="bg-gradient-to-br from-blue-50 to-purple-50 rounded-xl p-4 w-16 h-16 mx-auto mb-4 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
              <Icon name={stat.icon} class="h-8 w-8 text-blue-600" />
            </div>
            <div class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">
              {stat.number}
            </div>
            <div class="text-sm text-gray-600 leading-tight">
              {stat.label}
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Testimonials Carousel -->
    <div class="relative">
      
      <!-- Testimonials Container -->
      <div id="testimonials-container" class="overflow-hidden">
        <div id="testimonials-track" class="flex transition-transform duration-500 ease-in-out">
          
          {testimonials.map((testimonial, index) => (
            <div class="w-full md:w-1/2 lg:w-1/3 flex-shrink-0 px-4">
              <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-100 h-full relative hover:shadow-xl transition-all duration-300 hover:-translate-y-2">
                
                <!-- Quote Icon -->
                <div class="absolute top-6 right-6 opacity-10">
                  <Icon name="Quote" class="h-12 w-12 text-blue-600" />
                </div>
                
                <!-- Rating Stars -->
                <div class="flex mb-6">
                  {Array.from({ length: testimonial.rating }, (_, i) => (
                    <Icon name="Star" class="h-5 w-5 text-yellow-400 fill-current" />
                  ))}
                </div>

                <!-- Testimonial Content -->
                <blockquote class="text-gray-700 leading-relaxed mb-6">
                  "{testimonial.content}"
                </blockquote>

                <!-- Project Info -->
                <div class="mb-6 p-4 bg-gray-50 rounded-xl">
                  <p class="text-sm text-gray-600">
                    <strong>Proyecto:</strong> {testimonial.project}
                  </p>
                </div>

                 <!-- Client Info -->
                 <div class="flex items-center">
                   <div class="relative">
                     <img
                       src={testimonial.image}
                       alt={testimonial.name}
                       class="w-12 h-12 rounded-full object-cover border-2 border-white shadow-lg hover:scale-110 transition-transform duration-300"
                     />
                     <div class="absolute -bottom-1 -right-1 w-4 h-4 bg-green-400 border-2 border-white rounded-full"></div>
                   </div>
                   <div class="ml-4">
                     <div class="font-semibold text-gray-900 hover:text-blue-600 transition-colors duration-300">
                       {testimonial.name}
                     </div>
                     <div class="text-sm text-gray-600">
                       {testimonial.position} en {testimonial.company}
                     </div>
                   </div>
                </div>

                <!-- Verification Badge -->
                <div class="absolute bottom-4 right-4">
                  <div class="bg-green-100 text-green-700 px-2 py-1 rounded-full text-xs font-medium flex items-center">
                    <Icon name="ShieldCheck" class="h-3 w-3 mr-1" />
                    Verificado
                  </div>
                </div>
              </div>
            </div>
          ))}

        </div>
      </div>

      <!-- Navigation Arrows -->
      <button 
        id="prev-btn"
        class="absolute left-0 top-1/2 -translate-y-1/2 -translate-x-4 bg-white rounded-full p-3 shadow-lg border border-gray-200 hover:bg-gray-50 transition-all duration-200 hover:shadow-xl z-10"
        aria-label="Testimonio anterior"
      >
        <Icon name="ChevronLeft" class="h-6 w-6 text-gray-600" />
      </button>

      <button 
        id="next-btn"
        class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-4 bg-white rounded-full p-3 shadow-lg border border-gray-200 hover:bg-gray-50 transition-all duration-200 hover:shadow-xl z-10"
        aria-label="Siguiente testimonio"
      >
        <Icon name="ChevronRight" class="h-6 w-6 text-gray-600" />
      </button>

      <!-- Dots Indicator -->
      <div class="flex justify-center mt-8 space-x-2">
        {Array.from({ length: Math.ceil(testimonials.length / 3) }, (_, i) => (
          <button 
            class="dot w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-colors duration-200"
            data-slide={i}
            aria-label={`Ir a grupo ${i + 1} de testimonios`}
          ></button>
        ))}
      </div>

    </div>

    <!-- Call to Action -->
    <div class="text-center mt-16">
      <div class="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl p-8 lg:p-12 text-white relative overflow-hidden">
        
        <!-- Background decoration -->
        <div class="absolute top-0 left-0 right-0 bottom-0 opacity-10">
          <div class="absolute top-4 left-4 w-32 h-32 bg-white rounded-full"></div>
          <div class="absolute bottom-4 right-4 w-48 h-48 bg-white rounded-full"></div>
        </div>

        <div class="relative z-10 max-w-2xl mx-auto">
          <Icon name="Users" class="h-12 w-12 mx-auto mb-6 text-yellow-300" />
          <h3 class="text-2xl md:text-3xl font-bold mb-4">
            ¿Quieres ser nuestro próximo caso de éxito?
          </h3>
          <p class="text-blue-100 text-lg mb-8">
            Únete a más de 50 empresas que ya transformaron su negocio con nosotros. 
            Tu proyecto podría ser el siguiente testimonio destacado.
          </p>
          <a 
            href="#contacto"
            class="inline-flex items-center px-8 py-4 bg-white text-blue-600 font-semibold rounded-xl hover:bg-gray-50 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1"
          >
            Iniciar Mi Proyecto
            <Icon name="ArrowRight" class="ml-2 h-5 w-5" />
          </a>
        </div>
      </div>
    </div>

  </div>
</section>

<script>
  // Testimonials Carousel Logic
  document.addEventListener('DOMContentLoaded', function() {
    const track = document.getElementById('testimonials-track');
    const prevBtn = document.getElementById('prev-btn');
    const nextBtn = document.getElementById('next-btn');
    const dots = document.querySelectorAll('.dot');
    
    if (!track || !prevBtn || !nextBtn) return;

    let currentSlide = 0;
    const totalTestimonials = 6; // Total number of testimonials
    const itemsPerSlide = window.innerWidth >= 1024 ? 3 : window.innerWidth >= 768 ? 2 : 1;
    const maxSlides = Math.ceil(totalTestimonials / itemsPerSlide) - 1;

    function updateCarousel() {
      const slideWidth = 100 / itemsPerSlide;
      const translateX = -currentSlide * slideWidth * itemsPerSlide;
      track.style.transform = `translateX(${translateX}%)`;
      
      // Update dots
      dots.forEach((dot, index) => {
        if (index === Math.floor(currentSlide * itemsPerSlide / 3)) {
          dot.classList.remove('bg-gray-300');
          dot.classList.add('bg-blue-600');
        } else {
          dot.classList.remove('bg-blue-600');
          dot.classList.add('bg-gray-300');
        }
      });
    }

    function nextSlide() {
      currentSlide = currentSlide >= maxSlides ? 0 : currentSlide + 1;
      updateCarousel();
    }

    function prevSlide() {
      currentSlide = currentSlide <= 0 ? maxSlides : currentSlide - 1;
      updateCarousel();
    }

    // Event listeners
    nextBtn.addEventListener('click', nextSlide);
    prevBtn.addEventListener('click', prevSlide);

    // Dot navigation
    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        currentSlide = index;
        updateCarousel();
      });
    });

    // Auto-play
    let autoplayInterval = setInterval(nextSlide, 5000);

    // Pause autoplay on hover
    track.addEventListener('mouseenter', () => {
      clearInterval(autoplayInterval);
    });

    track.addEventListener('mouseleave', () => {
      autoplayInterval = setInterval(nextSlide, 5000);
    });

    // Handle resize
    window.addEventListener('resize', () => {
      const newItemsPerSlide = window.innerWidth >= 1024 ? 3 : window.innerWidth >= 768 ? 2 : 1;
      if (newItemsPerSlide !== itemsPerSlide) {
        location.reload(); // Simple approach for responsive changes
      }
    });

    // Initialize
    updateCarousel();
  });
</script>

<style>
  /* Smooth hover animations */
  .group:hover .group-hover\:scale-110 {
    transform: scale(1.1);
  }

  /* Testimonial card animations */
  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    #testimonials-track {
      flex-direction: column;
    }
    
    #testimonials-track > div {
      width: 100% !important;
      margin-bottom: 1rem;
    }
  }

  /* Stars animation */
  .star-animation {
    animation: starTwinkle 2s ease-in-out infinite alternate;
  }

  @keyframes starTwinkle {
    0% { opacity: 0.7; }
    100% { opacity: 1; }
  }
</style>